# Resolve PR #310 conflicts

- Branch: map-view-learned-skill-borders
- Base: origin/v4.10.0
- Merge the current PR base into the existing feature branch without rewriting history.
- Preserve learned/unlearned border controls and settings while integrating current map layout and resizing behavior.
- Review conflict resolutions and final diff, run available static checks, commit and push the merge.
- Verify GitHub reports the PR mergeable. In-game visual validation remains a separate requirement.

## Resolution

- The sole textual conflict was in `TravelMapTab:UpdateMapQuickslot`.
- Retain the base branch's offset-free Y coordinate and its updated X inset and layout lifecycle.
- Apply position and size to the feature's border frame, preserving joint icon/border scaling and border visibility settings.
- Review the resulting feature diff against v4.10.0 and check for whitespace errors and remaining conflict markers. Lua/luac are unavailable locally; in-game validation is pending.
